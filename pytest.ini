[pytest]
DJANGO_SETTINGS_MODULE = krx.settings.local
python_files = tests/test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests (no DB, no external services)
    integration: Integration tests (DB, real services)
